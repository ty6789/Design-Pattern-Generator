package 11package

//Abstract Container Class
public interface ${element}Aggregate {

    void add${element}(${element} element);

    void remove${element}(${element} element);

    ${element}Iterator get${element}Iterator();
}